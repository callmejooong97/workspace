/*
<진짜 무조건 나옴!!!!!!!!!!!!!!!!!>
명명규칭(naming rule)
1. 알파벳 대문자(A~Z), 소문자 (a~z), 숫자(0~9),특수문자(_)만 사용가능 
2. 숫자로 시작 불가 ex) 1ABC (X), ABC1 (O)
3. 대문자와 소문자는 다르게 인식 ex) ABC != Abc ! = abc (같지않음)
4. 예약어는 사용 불가 ex) 프로그래밍 언어에서 이미 사용하고 있는 키워드 (main,prinf,sizeof..)

<암기 강조 → 무조건 나와요!!!>
네이밍 케이스 (합성어 표현)
variable
method
class

예: student+number
1. 파스칼 케이스 → Age,StudentNumber (첫 문자를 대문자로 쓰는게 파스칼 케이스)
    - JAVA(C),Python(C)
2. 카멜 케이스 → age,studentNumber (첫 문자를 소문자로 쓰는 것)
    - C(V,M) , JAVA(V,M),JSON
3. 스네이크 케이스 → age,student_number
    - Python(V,M)
4. 케밥 케이스 → age,student-number
    - HTML,CSS
    
    JAVA 예시
    StudentNumber → class
    studentNumber() → Method
    studentNumber → Variable */
